[project]
name = "travel-buddy"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "google-generativeai",
    "pandas",
    "pydantic-ai",
    "streamlit>=1.40.0",
    "uvicorn",
    "lancedb",
    "firecrawl-py>=4.13.4",
    "httpx",  # Add this for async HTTP requests
]
