
  # Optional: Add database service later (PostgreSQL, etc.)
  # db:
  #   image: postgres:15
  #   environment:
  #     - POSTGRES_DB=travel_buddy
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:

services:
  backend:
    build:
      context: .
      dockerfile: docker/backend.Dockerfile
    ports:
      - "8000:8000"
  frontend:
    build:
      context: .
      dockerfile: docker/frontend.Dockerfile
    ports:
      - "8501:8501"

